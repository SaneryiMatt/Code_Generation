```xml
<instruction>
<task_description>
你的任务是根据提供的 JSON 数据生成对应的 Flask 模型类，每个模型类输出为独立的文件。
输出内容仅包含 Python 代码，代码块需用 ``` 包裹。

<instructions>
1. 解析输入的 JSON 数据，每个对象表示一个模型类，包含模型类名称、显示名称、描述及字段信息。
2. 为每个模型类生成一个独立的 Flask 模型类文件，包含模型类定义、表名、字段定义、创建时间、更新时间等。
3. 确保生成的代码符合 Flask 模型类规范，包括正确的类定义、字段类型、注释及命名规范。
4. 每个文件以 `model/table_name.py` 命名，表名基于模型类名称（小写）。
5. 输出代码应为纯 Python 代码，不包含任何 XML 标记或多余说明。

<output_format>
# model/table_name.py
```
code
```
</output_format>
</task_description>
</instruction>
```